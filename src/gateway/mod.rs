// Re-export everything from the oxicrab-gateway crate.
// This module exists so that the rest of the main crate can continue to
// use `crate::gateway::*` paths without changes.
pub use oxicrab_gateway::*;
